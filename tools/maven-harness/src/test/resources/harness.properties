# ─────────────────────────────────────────────────────────────
# Impilo vNext Integration Harness — Filtered Properties
# Values are injected by Maven resource filtering from
# -D flags, profile properties, or env vars.
# ─────────────────────────────────────────────────────────────

tenant.id=${tenantId}
pod.id=${podId}
facility.id=${facilityId}
workspace.id=${workspaceId}

base.url.tshepo=${baseUrlTshepo}
base.url.butano=${baseUrlButano}
base.url.msika=${baseUrlMsika}
base.url.kernel=${baseUrlKernel}

supabase.anon.key=${supabaseAnonKey}
