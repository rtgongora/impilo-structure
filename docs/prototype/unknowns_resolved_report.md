# Unknowns Resolved Report

> Generated from codebase inspection. All UNKNOWN/NOT OBSERVED entries traced to source.

| Doc File | Route/Component | What was UNKNOWN | What is now confirmed | Code Pointer(s) |
|----------|----------------|------------------|----------------------|-----------------|
| `api_surface_map.md` | PatientSearch | Patient search API call | Supabase query: `supabase.from("patients").select(...).or("first_name.ilike.%q%,last_name.ilike.%q%,mrn.ilike.%q%,phone_primary.ilike.%q%").limit(10)` with 300ms debounce | `src/components/search/PatientSearch.tsx:73-77` |
| `api_surface_map.md` | useDashboardData | Dashboard data queries | 6 parallel Supabase queries: (1) `encounters` with patient join, status=active, limit 20; (2) `clinical_orders` status in [pending, in_progress], limit 50; (3) `referrals` status in [pending, accepted, in_progress], limit 20; (4) `lab_results` with lab_order→patient join, limit 20; (5) count queries for stats (encounters, orders, alerts, completed today, results, referrals) | `src/hooks/useDashboardData.ts:94-301` |
| `api_surface_map.md` | Realtime subscriptions | Which tables have realtime enabled | 19 tables confirmed via migrations: `beds`, `shift_handoffs`, `clinical_messages`, `clinical_pages`, `voice_calls`, `teleconsult_signals`, `call_sessions`, `call_ice_candidates`, `idp_revocation_events`, `client_registry`, `client_duplicate_queue`, `sorting_sessions`, `lab_critical_alerts`, `posts`, `post_comments`, `campaign_donations`, `medication_schedule_times`, `client_queue_notifications`, `client_queue_requests`, `teleconsult_responses`, `trust_layer_consent`, `trust_layer_break_glass`, `landela_notifications` | `supabase/migrations/` (multiple files, see ALTER PUBLICATION statements) |
| `state_and_storage.md` | ShiftContext | Shift context details | Provides: `activeShift: ActiveShift \| null`, `isOnShift: boolean`, `shiftDuration: number` (minutes, updated every 60s), `loading`, `actionLoading`, `startShift(facilityId, workspaceId)`, `endShift(handoverNotes?, summary?)`, `transferWorkspace(workspaceId, reason, notes?)`, `refreshShift()`. Delegates to `useWorkspaceData()` hook. | `src/contexts/ShiftContext.tsx:1-100` |
| `state_and_storage.md` | ProviderContext | Provider context details | Provides: `provider: ProviderProfile` (mock: Dr. James Mwangi, physician, general-medicine, Parirenyatwa), `worklist: WorklistItem[]` (mock: 4 referral items), `stats: DashboardStats` (8 stat fields). Types: `ProviderRole` (10 roles), `Department` (16 departments), `ReferralStage` (8 stages), `ReferralStatus` (10 statuses). Currently uses hardcoded mock data. | `src/contexts/ProviderContext.tsx:1-308` |
| `state_and_storage.md` | PWA/Offline sync | Offline data sync strategy | Only `OfflineConflictResolver.tsx` exists in `src/components/sync/`. No full offline sync engine observed — conflict resolution UI component only. | `src/components/sync/OfflineConflictResolver.tsx` |
| `page_by_page_spec.md` | /patients | Patient list tabs | 3 tabs: "all" (default), "active" (filters `is_active=true`), "inactive" (filters `is_active=false`). Client-side filtering on the fetched patient array. | `src/pages/Patients.tsx:39,62-72` |
